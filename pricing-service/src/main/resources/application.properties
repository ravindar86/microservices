spring.application.name=pricing-service

#configuring the URL for Configuration Server
spring.cloud.config.uri=http://localhost:8888

### the below props moved to git repository and will be accessed via git cloud config server
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url = jdbc:mysql://localhost:3306/pricing_service_db

### credentials moved to vault
#spring.datasource.username = root
#spring.datasource.password = root

### the below props moved to git repository and will be accessed via git cloud config server
#spring.jpa.show-sql = true
#spring.jpa.hibernate.ddl-auto = update
#spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect
#spring.jpa.properties.hibernate.format_sql=true

server.port=8082
